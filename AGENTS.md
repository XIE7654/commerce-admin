# 项目协作约定

## 后端代码

- 后端项目位于本项目同级目录：`../commerce`。
- 涉及后端的新增、修改或删除代码时，必须在 `../commerce` 中完成；不要将后端实现写入 `commerce-admin`。

## API 调用

- 前端调用 API 时，默认调用与当前页面功能对应的同一个 Controller。
- 只有在接口职责明确属于其他 Controller，或现有接口约定要求时，才跨 Controller 调用。
- 涉及关联 `temu_shop.id` 时，统一使用店铺 API 接口获取或关联店铺数据。

## 修改范围

- 如无特殊要求，仅允许修改 Temu 和 Amazon 相关目录。
- 修改其他目录前，必须获得明确要求或授权。
